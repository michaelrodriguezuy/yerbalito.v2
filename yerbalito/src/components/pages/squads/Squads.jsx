import PlayersCard from "../../layout/player/PlayersCards";

const Squads = () => {
  return (
    <div
      className="page-container"
      style={{
        textAlign: "center",
        height: "100%",
        paddingBottom: "20px"
      }}
    >
      <PlayersCard />
    </div>
  );
};

export default Squads;
